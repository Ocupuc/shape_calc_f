name: shape_calc_f
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev. Remove this if you wish to publish your package.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # Убедитесь, что это включено и не закомментировано
    sdk: flutter
  http: ^0.13.3
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  generate: true  # Добавляем эту строку для поддержки локализации


